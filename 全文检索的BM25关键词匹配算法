BM25 是信息检索领域最经典的关键词匹配算法，用来给文档和查询之间的相关性打分。

核心思想
给定一个查询（比如"人均收入增长"），BM25 对每篇文档算一个分数。分数越高，文档越相关。分数取决于三个因素：

因素	                  含义	                   类比
词频（TF）	    查询词在文档中出现了几次	    一本书里"机器学习"这个词出现 50 次，这本书大概真的在讲机器学习
逆文档频率（IDF）查询词在整个文档库里有多罕见	"的"每篇都有，不值钱；"基尼系数"只有三篇有，很值钱
文档长度归一化	长文档天然词频高，需要打折	300页的书出现10次"机器学习" vs 10页的论文出现10次，后者更相关
公式直觉（不写数学）
BM25分数 = Σ (每个查询词的重要程度 × 这个词在这篇文档里的匹配程度)